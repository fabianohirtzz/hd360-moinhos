# Blog Fase 2 — Plano 1: Fundação Supabase + build lendo do Supabase

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Migrar a fonte de verdade do blog de `posts.json` para o Supabase, mantendo o build estático intacto: o `build.mjs` passa a ler os posts publicados do Supabase (com fallback para o JSON local), e os 23 posts atuais são semeados no banco.

**Architecture:** Camada nova `lib/supabase-map.mjs` (mapeadores puros entre o formato interno do post e a linha do banco) + `lib/load-posts.mjs` (escolhe entre Supabase via REST `fetch` e o JSON local pela presença de env vars). O `build.mjs` troca a leitura direta do JSON por `loadPosts()`. Os renderizadores e seus testes da Fase 1 ficam **inalterados** porque a fonte sempre entrega o mesmo formato interno. Esquema SQL + RLS + função de curtidas ficam versionados em `supabase/`.

**Tech Stack:** Node 18+ (ESM, `node:test`, `fetch` nativo — zero dependência nova no tooling), Supabase (Postgres + PostgREST + RLS).

**Pré-requisito de ambiente:** As tarefas 2–4 e 6 rodam offline (fixtures, sem rede). As tarefas 1 (aplicar SQL) e 5 (rodar o seed) precisam do projeto Supabase criado e das env vars `SUPABASE_URL` e `SUPABASE_SERVICE_KEY`. Quando chegar nelas, peça ao usuário pra criar o projeto HD360 no Supabase e fornecer as chaves.

**Formato interno do post** (o que os renderizadores esperam, já gravado em `assets/blog/posts.json`):
```
{ id, slug, title, date, modified, dateLabel, category:{name,color},
  coverImage, excerpt, content, metaDescription, seoTitle, ogImage,
  focusKeyword, tags }
```

---

### Task 1: Esquema SQL, RLS e função de curtidas

**Files:**
- Create: `supabase/schema.sql`
- Create: `supabase/README.md`

- [ ] **Step 1: Escrever o esquema completo**

Criar `supabase/schema.sql`:

```sql
-- HD360 blog — esquema da Fase 2.
-- Aplicar no SQL Editor do projeto Supabase (uma vez).

create table if not exists public.posts (
  id               bigint generated by default as identity primary key,
  slug             text not null unique,
  title            text not null,
  date             timestamptz not null default now(),
  modified         timestamptz not null default now(),
  category_name    text not null,
  category_color   text not null,
  cover_image      text default '',
  excerpt          text default '',
  content          text default '',
  meta_description text default '',
  seo_title        text default '',
  og_image         text default '',
  focus_keyword    text default '',
  tags             text[] not null default '{}',
  likes            integer not null default 0,
  status           text not null default 'draft' check (status in ('draft','published')),
  created_at       timestamptz not null default now(),
  updated_at       timestamptz not null default now()
);

create index if not exists posts_status_idx on public.posts (status);
create index if not exists posts_date_idx on public.posts (date desc);

-- updated_at automático
create or replace function public.touch_updated_at()
returns trigger language plpgsql as $$
begin
  new.updated_at = now();
  return new;
end; $$;

drop trigger if exists posts_touch_updated_at on public.posts;
create trigger posts_touch_updated_at
  before update on public.posts
  for each row execute function public.touch_updated_at();

-- Curtidas: incremento anônimo seguro, sem ler/alterar mais nada.
create or replace function public.increment_likes(p_slug text)
returns integer
language plpgsql
security definer
set search_path = public
as $$
declare new_count integer;
begin
  update public.posts
     set likes = likes + 1
   where slug = p_slug and status = 'published'
   returning likes into new_count;
  return coalesce(new_count, 0);
end; $$;

-- RLS
alter table public.posts enable row level security;

-- Leitura pública só de posts publicados.
drop policy if exists posts_public_read on public.posts;
create policy posts_public_read on public.posts
  for select using (status = 'published');

-- Escrita só por usuário autenticado (o admin).
drop policy if exists posts_admin_all on public.posts;
create policy posts_admin_all on public.posts
  for all to authenticated
  using (true) with check (true);

-- Permissão de execução da função de curtidas para anônimos.
grant execute on function public.increment_likes(text) to anon, authenticated;
```

> Nota: o build na Action usa a **service key**, que ignora RLS — por isso lê posts em qualquer status. O `fromSupabase` filtra `status=published` na query de qualquer forma.

- [ ] **Step 2: Documentar a aplicação e o Storage**

Criar `supabase/README.md`:

```markdown
# Supabase — HD360 blog

## Aplicar o esquema
1. Criar projeto no Supabase (região mais próxima do BR).
2. SQL Editor → colar e rodar `schema.sql`.
3. Storage → criar bucket público `blog-images` (capa e imagens inline).
4. Authentication → criar 1 usuário (e-mail/senha) para o admin.

## Chaves
- `anon key` → usada no painel (`/painel/`) e nas curtidas (pública por design).
- `service key` → segredo; só nas GitHub Actions e no seed local. Nunca no front.

## Env vars (build/seed local e CI)
- `SUPABASE_URL`   = https://<ref>.supabase.co
- `SUPABASE_SERVICE_KEY` = <service role key>
```

- [ ] **Step 3: Commit**

```bash
git add supabase/schema.sql supabase/README.md
git commit -m "feat(blog): esquema Supabase, RLS e funcao de curtidas (Fase 2)"
```

---

### Task 2: Mapeador formato interno → linha do banco (`toSupabaseRow`)

**Files:**
- Create: `tools/blog-migration/lib/supabase-map.mjs`
- Test: `tools/blog-migration/test/supabase-map.test.mjs`

- [ ] **Step 1: Escrever o teste que falha**

Criar `tools/blog-migration/test/supabase-map.test.mjs`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { toSupabaseRow } from '../lib/supabase-map.mjs';

const internalPost = {
  id: 1068,
  slug: 'o-que-esperar-da-clinica',
  title: 'O Que Esperar da Clínica?',
  date: '2026-02-04T14:09:44',
  modified: '2026-02-05T10:00:00',
  dateLabel: '4 de fevereiro de 2026',
  category: { name: 'Terapias e Abordagens', color: 'azul' },
  coverImage: 'images/blog/capa.png',
  excerpt: 'Resumo curto.',
  content: '<p>Corpo</p>',
  metaDescription: 'Meta.',
  seoTitle: 'O Que Esperar | HD360',
  ogImage: 'images/blog/capa.png',
  focusKeyword: 'clínica autismo',
  tags: ['TEA', 'Família'],
};

test('toSupabaseRow achata o formato interno para colunas do banco', () => {
  const row = toSupabaseRow(internalPost);
  assert.equal(row.slug, 'o-que-esperar-da-clinica');
  assert.equal(row.category_name, 'Terapias e Abordagens');
  assert.equal(row.category_color, 'azul');
  assert.equal(row.cover_image, 'images/blog/capa.png');
  assert.equal(row.meta_description, 'Meta.');
  assert.equal(row.seo_title, 'O Que Esperar | HD360');
  assert.equal(row.og_image, 'images/blog/capa.png');
  assert.equal(row.focus_keyword, 'clínica autismo');
  assert.deepEqual(row.tags, ['TEA', 'Família']);
  assert.equal(row.status, 'published'); // padrão do seed
  // dateLabel NÃO é coluna (é derivado na leitura)
  assert.equal('dateLabel' in row, false);
  assert.equal('category' in row, false);
});

test('toSupabaseRow aceita status explícito e tolera campos ausentes', () => {
  const row = toSupabaseRow({ ...internalPost, coverImage: undefined, tags: undefined }, { status: 'draft' });
  assert.equal(row.status, 'draft');
  assert.equal(row.cover_image, '');
  assert.deepEqual(row.tags, []);
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `node --test tools/blog-migration/test/supabase-map.test.mjs`
Expected: FAIL — `Cannot find module '../lib/supabase-map.mjs'`.

- [ ] **Step 3: Implementar `toSupabaseRow`**

Criar `tools/blog-migration/lib/supabase-map.mjs`:

```js
import { formatDatePtBr } from './format-date.mjs';

// Formato interno do post (usado pelos renderizadores) -> linha da tabela `posts`.
// `dateLabel` é derivado na leitura, então não vai pro banco.
export function toSupabaseRow(post, { status = 'published' } = {}) {
  return {
    id: post.id,
    slug: post.slug,
    title: post.title,
    date: post.date,
    modified: post.modified || post.date,
    category_name: post.category.name,
    category_color: post.category.color,
    cover_image: post.coverImage || '',
    excerpt: post.excerpt || '',
    content: post.content || '',
    meta_description: post.metaDescription || '',
    seo_title: post.seoTitle || '',
    og_image: post.ogImage || '',
    focus_keyword: post.focusKeyword || '',
    tags: post.tags || [],
    status,
  };
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `node --test tools/blog-migration/test/supabase-map.test.mjs`
Expected: PASS (2 testes).

- [ ] **Step 5: Commit**

```bash
git add tools/blog-migration/lib/supabase-map.mjs tools/blog-migration/test/supabase-map.test.mjs
git commit -m "feat(blog): mapeador toSupabaseRow (formato interno -> linha do banco)"
```

---

### Task 3: Mapeador linha do banco → formato interno (`mapSupabaseRow`)

**Files:**
- Modify: `tools/blog-migration/lib/supabase-map.mjs`
- Modify: `tools/blog-migration/test/supabase-map.test.mjs`

- [ ] **Step 1: Adicionar o teste que falha**

Acrescentar ao fim de `tools/blog-migration/test/supabase-map.test.mjs`:

```js
import { mapSupabaseRow } from '../lib/supabase-map.mjs';

const dbRow = {
  id: 1068,
  slug: 'o-que-esperar-da-clinica',
  title: 'O Que Esperar da Clínica?',
  date: '2026-02-04T14:09:44',
  modified: '2026-02-05T10:00:00',
  category_name: 'Terapias e Abordagens',
  category_color: 'azul',
  cover_image: 'images/blog/capa.png',
  excerpt: 'Resumo curto.',
  content: '<p>Corpo</p>',
  meta_description: 'Meta.',
  seo_title: 'O Que Esperar | HD360',
  og_image: 'images/blog/capa.png',
  focus_keyword: 'clínica autismo',
  tags: ['TEA', 'Família'],
  likes: 7,
  status: 'published',
};

test('mapSupabaseRow reconstrói o formato interno e deriva dateLabel', () => {
  const p = mapSupabaseRow(dbRow);
  assert.equal(p.slug, 'o-que-esperar-da-clinica');
  assert.deepEqual(p.category, { name: 'Terapias e Abordagens', color: 'azul' });
  assert.equal(p.coverImage, 'images/blog/capa.png');
  assert.equal(p.metaDescription, 'Meta.');
  assert.equal(p.seoTitle, 'O Que Esperar | HD360');
  assert.equal(p.ogImage, 'images/blog/capa.png');
  assert.equal(p.focusKeyword, 'clínica autismo');
  assert.deepEqual(p.tags, ['TEA', 'Família']);
  assert.equal(p.dateLabel, '4 de fevereiro de 2026'); // derivado
  assert.equal(p.likes, 7);
});

test('roundtrip toSupabaseRow -> mapSupabaseRow preserva os campos do renderizador', () => {
  const p = mapSupabaseRow({ ...toSupabaseRow(internalPost), likes: 0 });
  assert.equal(p.title, internalPost.title);
  assert.deepEqual(p.category, internalPost.category);
  assert.equal(p.content, internalPost.content);
  assert.deepEqual(p.tags, internalPost.tags);
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `node --test tools/blog-migration/test/supabase-map.test.mjs`
Expected: FAIL — `mapSupabaseRow` não exportado.

- [ ] **Step 3: Implementar `mapSupabaseRow`**

Acrescentar a `tools/blog-migration/lib/supabase-map.mjs`:

```js
// Linha da tabela `posts` -> formato interno do post (com dateLabel derivado).
export function mapSupabaseRow(row) {
  return {
    id: row.id,
    slug: row.slug,
    title: row.title,
    date: row.date,
    modified: row.modified || row.date,
    dateLabel: formatDatePtBr(row.date),
    category: { name: row.category_name, color: row.category_color },
    coverImage: row.cover_image || '',
    excerpt: row.excerpt || '',
    content: row.content || '',
    metaDescription: row.meta_description || '',
    seoTitle: row.seo_title || '',
    ogImage: row.og_image || '',
    focusKeyword: row.focus_keyword || '',
    tags: row.tags || [],
    likes: row.likes || 0,
  };
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `node --test tools/blog-migration/test/supabase-map.test.mjs`
Expected: PASS (4 testes).

- [ ] **Step 5: Commit**

```bash
git add tools/blog-migration/lib/supabase-map.mjs tools/blog-migration/test/supabase-map.test.mjs
git commit -m "feat(blog): mapeador mapSupabaseRow (linha do banco -> formato interno)"
```

---

### Task 4: Carregador de posts (`loadPosts`: Supabase via REST ou JSON local)

**Files:**
- Create: `tools/blog-migration/lib/load-posts.mjs`
- Test: `tools/blog-migration/test/load-posts.test.mjs`

- [ ] **Step 1: Escrever o teste que falha**

Criar `tools/blog-migration/test/load-posts.test.mjs`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { writeFile, mkdtemp, rm } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { fromJson, fromSupabase, loadPosts } from '../lib/load-posts.mjs';

const sampleRow = {
  id: 1, slug: 'post-a', title: 'Post A',
  date: '2026-02-04T14:09:44', modified: '2026-02-04T14:09:44',
  category_name: 'Terapias e Abordagens', category_color: 'azul',
  cover_image: '', excerpt: '', content: '<p>a</p>',
  meta_description: '', seo_title: '', og_image: '', focus_keyword: '',
  tags: ['TEA'], likes: 3, status: 'published',
};

test('fromJson lê e devolve o array do arquivo', async () => {
  const dir = await mkdtemp(join(tmpdir(), 'lp-'));
  const file = join(dir, 'posts.json');
  await writeFile(file, JSON.stringify([{ slug: 'x', title: 'X' }]));
  const posts = await fromJson(file);
  assert.equal(posts[0].slug, 'x');
  await rm(dir, { recursive: true, force: true });
});

test('fromSupabase consulta só publicados e mapeia as linhas', async () => {
  let calledUrl = '';
  const fakeFetch = async (url, opts) => {
    calledUrl = url;
    assert.equal(opts.headers.apikey, 'svc');
    return { ok: true, json: async () => [sampleRow] };
  };
  const posts = await fromSupabase({ url: 'https://x.supabase.co', serviceKey: 'svc', fetchImpl: fakeFetch });
  assert.match(calledUrl, /status=eq\.published/);
  assert.equal(posts[0].slug, 'post-a');
  assert.deepEqual(posts[0].category, { name: 'Terapias e Abordagens', color: 'azul' });
  assert.equal(posts[0].dateLabel, '4 de fevereiro de 2026'); // já mapeado
});

test('fromSupabase lança erro em resposta não-ok', async () => {
  const fakeFetch = async () => ({ ok: false, status: 401, text: async () => 'no' });
  await assert.rejects(() => fromSupabase({ url: 'u', serviceKey: 'k', fetchImpl: fakeFetch }), /401/);
});

test('loadPosts usa Supabase quando há env vars, senão o JSON', async () => {
  const fakeFetch = async () => ({ ok: true, json: async () => [sampleRow] });
  const viaSb = await loadPosts({ env: { SUPABASE_URL: 'u', SUPABASE_SERVICE_KEY: 'svc' }, fetchImpl: fakeFetch });
  assert.equal(viaSb[0].slug, 'post-a');

  const dir = await mkdtemp(join(tmpdir(), 'lp-'));
  const file = join(dir, 'posts.json');
  await writeFile(file, JSON.stringify([{ slug: 'json-fallback' }]));
  const viaJson = await loadPosts({ env: {}, jsonUrl: file });
  assert.equal(viaJson[0].slug, 'json-fallback');
  await rm(dir, { recursive: true, force: true });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `node --test tools/blog-migration/test/load-posts.test.mjs`
Expected: FAIL — `Cannot find module '../lib/load-posts.mjs'`.

- [ ] **Step 3: Implementar `load-posts.mjs`**

Criar `tools/blog-migration/lib/load-posts.mjs`:

```js
import { readFile } from 'node:fs/promises';
import { mapSupabaseRow } from './supabase-map.mjs';

// Lê do JSON local (semente / fallback). `jsonUrl` aceita caminho ou URL de arquivo.
export async function fromJson(jsonUrl) {
  return JSON.parse(await readFile(jsonUrl, 'utf8'));
}

// Lê posts publicados do Supabase via PostgREST com a service key. Zero dependência.
export async function fromSupabase({ url, serviceKey, fetchImpl = fetch }) {
  const endpoint = `${url}/rest/v1/posts?status=eq.published&select=*`;
  const res = await fetchImpl(endpoint, {
    headers: { apikey: serviceKey, Authorization: `Bearer ${serviceKey}` },
  });
  if (!res.ok) {
    const body = await res.text();
    throw new Error(`Supabase ${res.status}: ${body}`);
  }
  const rows = await res.json();
  return rows.map(mapSupabaseRow);
}

// Decide a fonte: Supabase se as env vars existirem, senão o JSON.
export async function loadPosts({ env = process.env, jsonUrl, fetchImpl } = {}) {
  if (env.SUPABASE_URL && env.SUPABASE_SERVICE_KEY) {
    return fromSupabase({ url: env.SUPABASE_URL, serviceKey: env.SUPABASE_SERVICE_KEY, fetchImpl });
  }
  return fromJson(jsonUrl);
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `node --test tools/blog-migration/test/load-posts.test.mjs`
Expected: PASS (4 testes).

- [ ] **Step 5: Commit**

```bash
git add tools/blog-migration/lib/load-posts.mjs tools/blog-migration/test/load-posts.test.mjs
git commit -m "feat(blog): loadPosts le do Supabase (REST) com fallback pro JSON"
```

---

### Task 5: Script de seed (posts.json → Supabase)

**Files:**
- Create: `tools/blog-migration/seed-supabase.mjs`

> A transformação (`toSupabaseRow`) já está coberta por teste na Task 2. Esta tarefa é o script de integração que faz o upsert via REST. O passo de execução (Step 3) precisa do projeto Supabase criado.

- [ ] **Step 1: Escrever o script de seed**

Criar `tools/blog-migration/seed-supabase.mjs`:

```js
import { readFile } from 'node:fs/promises';
import { toSupabaseRow } from './lib/supabase-map.mjs';

const ROOT = new URL('../../', import.meta.url);
const url = process.env.SUPABASE_URL;
const key = process.env.SUPABASE_SERVICE_KEY;

if (!url || !key) {
  console.error('Defina SUPABASE_URL e SUPABASE_SERVICE_KEY.');
  process.exit(1);
}

async function main() {
  const posts = JSON.parse(await readFile(new URL('assets/blog/posts.json', ROOT), 'utf8'));
  // Os 23 posts existentes entram como publicados.
  const rows = posts.map(p => toSupabaseRow(p, { status: 'published' }));

  const res = await fetch(`${url}/rest/v1/posts?on_conflict=slug`, {
    method: 'POST',
    headers: {
      apikey: key,
      Authorization: `Bearer ${key}`,
      'Content-Type': 'application/json',
      Prefer: 'resolution=merge-duplicates,return=minimal',
    },
    body: JSON.stringify(rows),
  });

  if (!res.ok) {
    console.error('Falha no seed:', res.status, await res.text());
    process.exit(1);
  }
  console.log(`Seed ok: ${rows.length} posts no Supabase.`);
}

main().catch(e => { console.error(e); process.exit(1); });
```

- [ ] **Step 2: Commit do script**

```bash
git add tools/blog-migration/seed-supabase.mjs
git commit -m "feat(blog): script de seed dos posts no Supabase"
```

- [ ] **Step 3: Rodar o seed contra o projeto (precisa do Supabase criado)**

Pré: projeto criado, `schema.sql` aplicado (Task 1), env vars definidas.

PowerShell:
```powershell
$env:SUPABASE_URL = 'https://<ref>.supabase.co'
$env:SUPABASE_SERVICE_KEY = '<service key>'
node tools/blog-migration/seed-supabase.mjs
```
Expected: `Seed ok: 23 posts no Supabase.`
Conferir no Supabase (Table editor → posts): 23 linhas, todas `status=published`.

---

### Task 6: Adaptar `build.mjs` para usar `loadPosts`

**Files:**
- Modify: `tools/blog-migration/build.mjs:24`

- [ ] **Step 1: Trocar a leitura direta do JSON pelo carregador**

Em `tools/blog-migration/build.mjs`, adicionar o import no topo (junto aos outros):

```js
import { loadPosts } from './lib/load-posts.mjs';
```

E substituir a linha 24:

```js
  const posts = JSON.parse(await readFile(new URL('assets/blog/posts.json', ROOT), 'utf8'));
```

por:

```js
  const posts = await loadPosts({ jsonUrl: new URL('assets/blog/posts.json', ROOT) });
```

> O `readFile` continua importado e usado pelo `injectBetween`, então o import permanece.

- [ ] **Step 2: Rodar o build sem env vars (deve usar o JSON, saída idêntica à de hoje)**

Run: `node tools/blog-migration/build.mjs`
Expected: imprime `blog.html: carrossel com 23 slides.`, `blog-todos.html: grade com 23 cards.` e as 23 páginas `ok`.

- [ ] **Step 3: Verificar que o build não alterou nada (saída byte a byte igual)**

Run: `git status --porcelain`
Expected: vazio (o build a partir do JSON reproduz exatamente os HTML já commitados). Se houver diff, investigar antes de seguir.

- [ ] **Step 4: Rodar a suíte de testes inteira (renderizadores intactos + novos mapeadores)**

Run: `node --test tools/blog-migration/test/`
Expected: todos verdes, incluindo os testes da Fase 1 e os novos (`supabase-map`, `load-posts`).

- [ ] **Step 5: Commit**

```bash
git add tools/blog-migration/build.mjs
git commit -m "refactor(blog): build le posts via loadPosts (Supabase ou JSON)"
```

- [ ] **Step 6: (Integração) Rodar o build lendo do Supabase**

Pré: seed rodado (Task 5).

PowerShell:
```powershell
$env:SUPABASE_URL = 'https://<ref>.supabase.co'
$env:SUPABASE_SERVICE_KEY = '<service key>'
node tools/blog-migration/build.mjs
git status --porcelain
```
Expected: build conclui com 23 slides/cards; `git status` permanece vazio (os posts do Supabase, vindos do seed, produzem o mesmo HTML que o JSON). Confirma que a fonte Supabase é equivalente. Limpar as env vars depois.

---

## Verificação do Plano 1

1. `node --test tools/blog-migration/test/` — tudo verde (Fase 1 + `supabase-map` + `load-posts`).
2. `node tools/blog-migration/build.mjs` sem env vars: gera os HTML a partir do JSON, `git status` limpo.
3. Com `schema.sql` aplicado e seed rodado: 23 posts publicados no Supabase.
4. `node tools/blog-migration/build.mjs` com env vars do Supabase: build equivalente (git status limpo).
5. Renderizadores e testes da Fase 1 **não foram modificados**.
6. Nada de travessões em comentários/copys novos.

## Próximos planos da Fase 2 (serão detalhados ao chegar)
- **Plano 2** — Painel `/painel/`: login (Supabase Auth), lista de posts, editor WYSIWYG, campos de SEO, tags curadas, upload de imagens (Storage).
- **Plano 3** — Pipeline de publicação: Edge Function (`repository_dispatch` com PAT), workflow `publish-blog.yml`, botão "Atualizar site" no painel + indicador de status.
- **Plano 4** — Curtidas no público: widget de coração no `/<slug>/`, chamada a `increment_likes`, de-dup por `localStorage`, número inicial renderizado no build.
