# OS / editor junk
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*~

# Dependencies (caso usemos Node mais adiante)
node_modules/
npm-debug.log*
yarn-error.log*

# Node usado só para otimizar mídia (ffmpeg-static) — não faz parte do site
package.json
package-lock.json
# Exceção: o painel admin precisa do seu package.json versionado (define "type":"module"
# para os testes ESM do painel e o CI da Fase 2).
!painel/package.json
# Exceção: os scripts do site (curtidas) usam ESM testável; package.json define "type":"module".
!assets/js/package.json

# Previews / rascunhos temporários
_*.png
_*.mjs
_*.html

# Build output
dist/
.cache/

# Env / segredos
.env
.env.local

# Cache local da Supabase CLI
supabase/.temp/
